query TeamIssues($first: Int!, $after: String, $teamKey: String!, $statusName: String!, $labelNames: [String!]!) {
  issues(
    first: $first
    after: $after
    filter: {
      team: { key: { eq: $teamKey } }
      state: { name: { eq: $statusName } }
      labels: { name: { in: $labelNames } }
    }
  ) {
    nodes {
      {{ issueFields }}
    }
    pageInfo {
      hasNextPage
      endCursor
    }
  }
}
