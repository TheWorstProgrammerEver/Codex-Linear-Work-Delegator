query TeamIssues($id: String!, $first: Int!, $statusName: String!) {
  team(id: $id) {
    issues(
      first: $first
      filter: {
        state: { name: { eq: $statusName } }
      }
    ) {
      nodes {
        {{ issueFields }}
      }
    }
  }
}
