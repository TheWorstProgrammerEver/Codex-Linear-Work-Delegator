id
identifier
title
url
description
priority
priorityLabel
createdAt
updatedAt
state { id name type }
labels { nodes { id name parent { id name } } }
team { id key name }
assignee { id name email }
creator { id name email }
project { id name }
cycle { id name }
relations(first: 50) {
  nodes {
    id
    type
    issue { identifier title url state { id name type } }
    relatedIssue { identifier title url state { id name type } }
  }
}
inverseRelations(first: 50) {
  nodes {
    id
    type
    issue { identifier title url state { id name type } }
    relatedIssue { identifier title url state { id name type } }
  }
}
