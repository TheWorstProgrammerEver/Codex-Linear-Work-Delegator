query ReviewFilteredIssuesAllTeams($first: Int!, $statusName: String!, $labelNames: [String!]!) {
  issues(
    first: $first
    filter: {
      state: { name: { eq: $statusName } }
      labels: { name: { in: $labelNames } }
    }
  ) {
    nodes {
      id
      identifier
      title
      url
      priority
      priorityLabel
      createdAt
      updatedAt
      state { id name type }
      labels { nodes { id name parent { id name } } }
      team { id key name }
    }
  }
}
