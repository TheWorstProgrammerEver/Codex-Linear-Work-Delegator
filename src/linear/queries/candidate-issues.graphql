query CandidateIssues($first: Int!, $statusName: String!) {
  issues(
    first: $first
    filter: {
      state: { name: { eq: $statusName } }
    }
  ) {
    nodes {
      {{ issueFields }}
    }
  }
}
